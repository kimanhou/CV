import React from "react";
import { SocialIcon, SocialMediaEnum } from "./SocialIcon";

export const SocialIconLinkedin: React.FC = (props) => {
    return <SocialIcon media={SocialMediaEnum.LINKEDIN} />;
};
